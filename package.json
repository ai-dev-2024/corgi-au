{
  "name": "corgi-au",
  "version": "0.1.0",
  "description": "Australia + global VIN decoding and EV charging station lookup on Cloudflare Workers, built on @cardog/corgi and @cardog/ocm-client.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@cardog/corgi": "2.0.4",
    "@cardog/ocm-client": "0.1.1",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.100.0"
  }
}
